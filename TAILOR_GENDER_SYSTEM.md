# نظام تحديد جنس الخياط (Gender Specialization System)

## نظرة عامة
تم إضافة نظام لتحديد تخصص الخياط (رجالي أو نسائي) بشكل إلزامي عند التسجيل، مع فلترة المنتجات والتصنيفات حسب الجنس.

## التغييرات الرئيسية

### 1. أنواع البيانات (types.ts)
```typescript
export type TailorGender = 'male' | 'female'; // تخصص الخياط: رجالي أو نسائي

// تم إضافة الحقل في User
export interface User {
  tailorGender?: TailorGender; // تخصص الخياط: رجالي أو نسائي (إلزامي للخياطين)
  // ... باقي الحقول
}

// تم إضافة الحقل في Shop
export interface Shop {
  tailorGender?: TailorGender; // تخصص الخياط: رجالي أو نسائي
  // ... باقي الحقول
}
```

### 2. نموذج التسجيل (AuthModal.tsx)
- ✅ إضافة اختيار جنس الخياط (رجالي/نسائي) بشكل إلزامي
- ✅ واجهة جميلة بزرين: "👔 خياطة رجالية" و "👗 خياطة نسائية"
- ✅ التحقق من اختيار الجنس قبل إتمام التسجيل
- ✅ عدم السماح بالتسجيل بدون اختيار الجنس

```tsx
// مثال على واجهة الاختيار
<button
  type="button"
  onClick={() => setTailorGender('male')}
  className={tailorGender === 'male' ? 'active' : ''}
>
  👔 خياطة رجالية
</button>
```

### 3. فلترة التصنيفات (TailorCollections.tsx)
- ✅ فلترة التصنيفات تلقائياً حسب جنس الخياط
- ✅ الخياط الرجالي: يرى فقط "الملابس الرجالية" وأبناءها (دشداشة، وزار، قميص، إلخ)
- ✅ الخياط النسائي: يرى فقط "الملابس النسائية" وأبناءها (عباية، فستان، جلابية، إلخ)
- ✅ لا يتم عرض التصنيفات غير المناسبة للجنس

```typescript
// مثال على الفلترة
level1Snapshot.docs.forEach(doc => {
  const nameAr = data.nameAr;
  
  // فلترة حسب جنس الخياط
  if (tailorGender === 'male' && nameAr === 'الملابس النسائية') {
    return; // تجاهل التصنيفات النسائية للخياط الرجالي
  }
  if (tailorGender === 'female' && nameAr === 'الملابس الرجالية') {
    return; // تجاهل التصنيفات الرجالية للخياط النسائي
  }
});
```

### 4. ملف تعريف الخياط (TailorProfile.tsx)
- ✅ عرض شارة التخصص بجانب اسم الخياط
- ✅ تصميم مميز: "👔 رجالي" باللون الأزرق أو "👗 نسائي" باللون الوردي
- ✅ مرئي بوضوح للزوار

### 5. حساب الخياط (TailorAccount.tsx)
- ✅ عرض نوع الخياطة في قسم "معلومات إضافية"
- ✅ شارة ملونة تظهر التخصص
- ✅ يمكن للخياط رؤية تخصصه الحالي

### 6. قائمة الخياطين (TailorList.tsx)
- ✅ عرض أيقونة التخصص بجانب اسم كل خياط
- ✅ أيقونة صغيرة: 👔 للرجالي، 👗 للنسائي
- ✅ سهولة التمييز البصري للمستخدمين

### 7. خدمات Firebase (firebase.ts)
- ✅ حفظ `tailorGender` في Firestore عند التسجيل
- ✅ قراءة `tailorGender` عند جلب بيانات المستخدم
- ✅ تضمين الحقل في جميع العمليات

### 8. Context (AppContext.tsx)
- ✅ إضافة `tailorGender` في `MerchantInfo` interface
- ✅ تمرير البيانات من نموذج التسجيل إلى Firebase

## سير العمل (Workflow)

### عند تسجيل خياط جديد:
1. المستخدم يختار "خياط" من قائمة الأدوار
2. يظهر اختيار إلزامي: رجالي أو نسائي
3. لا يمكن إتمام التسجيل بدون اختيار
4. يتم حفظ الاختيار في Firestore

### عند عرض المنتجات:
1. النظام يجلب `tailorGender` من بيانات المستخدم
2. يصفي التصنيفات Level 1 (الآباء) حسب الجنس
3. يعرض فقط التصنيفات Level 2 (الأبناء) التابعة للآباء المفلترين
4. الخياط لا يرى تصنيفات غير مناسبة لجنسه

### عند زيارة ملف الخياط:
1. يظهر التخصص بشكل واضح بجانب الاسم
2. شارة ملونة تساعد على التمييز البصري
3. المستخدمون يعرفون فوراً تخصص الخياط

## التصنيفات المتأثرة

### للخياطين الرجاليين (male):
- ✅ الملابس الرجالية
  - دشداشة
  - وزار
  - قميص
  - بدلة
  - إلخ...

### للخياطين النسائيين (female):
- ✅ الملابس النسائية
  - عباية
  - فستان
  - الجلابية
  - إلخ...

## ملاحظات مهمة

### للخياطين الحاليين (قبل التحديث):
- الخياطين الذين سجلوا قبل هذا التحديث لن يكون لديهم `tailorGender`
- سيتم عرض جميع التصنيفات لهم (سلوك افتراضي)
- يمكن إضافة سكريبت migration لتحديث البيانات القديمة

### للتطوير المستقبلي:
- ❌ لم يتم إضافة "أطفال" حالياً (حسب الطلب)
- يمكن إضافة `TailorGender = 'male' | 'female' | 'children'` مستقبلاً
- يمكن إضافة خيار "الكل" للخياطين الذين يخدمون الجنسين

### الأمان:
- ✅ التحقق من الاختيار في Frontend (AuthModal)
- ✅ حفظ البيانات في Firestore
- يُنصح بإضافة قاعدة في firestore.rules للتحقق من الحقل

## اختبار النظام

### سيناريوهات الاختبار:
1. ✅ تسجيل خياط رجالي → يرى فقط التصنيفات الرجالية
2. ✅ تسجيل خياط نسائي → يرى فقط التصنيفات النسائية
3. ✅ محاولة التسجيل بدون اختيار → رسالة خطأ
4. ✅ عرض ملف الخياط → شارة التخصص ظاهرة
5. ✅ قائمة الخياطين → أيقونات التخصص ظاهرة

## الملفات المعدلة

1. `types.ts` - إضافة TailorGender type
2. `components/AuthModal.tsx` - واجهة اختيار الجنس
3. `pages/TailorCollections.tsx` - فلترة التصنيفات
4. `pages/TailorProfile.tsx` - عرض شارة التخصص
5. `pages/accounts/TailorAccount.tsx` - عرض التخصص في الحساب
6. `pages/TailorList.tsx` - عرض أيقونات التخصص
7. `context/AppContext.tsx` - إضافة tailorGender في MerchantInfo
8. `services/firebase.ts` - حفظ وقراءة tailorGender
9. `services/mockService.ts` - دعم tailorGender في البيانات التجريبية

## تاريخ التحديث
تاريخ الإضافة: 9 ديسمبر 2025
