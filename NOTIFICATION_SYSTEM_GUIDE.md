# نظام الإشعارات والاستلام عند جاهزية الطلب

## 📋 نظرة عامة

تم تطوير نظام متكامل لإدارة إشعارات جاهزية الطلبات واختيار طريقة الاستلام.

---

## ✨ الميزات الرئيسية

### 1️⃣ إشعارات تلقائية عند الجاهزية
عندما يقوم الخياط بتغيير حالة الطلب إلى "جاهز"، يتلقى العميل إشعارات عبر:

- **📱 واتساب**: رسالة نصية مباشرة
- **📧 بريد إلكتروني**: رسالة HTML منسقة مع التفاصيل الكاملة
- **📲 SMS** (اختياري): للحالات الطارئة

### 2️⃣ اختيار طريقة الاستلام
يمكن للعميل اختيار إحدى طريقتين:

#### 🏪 الاستلام من المحل
- زيارة المحل مباشرة
- استلام فوري
- بدون رسوم إضافية

#### 🚚 التوصيل للمنزل
- توصيل للعنوان المحدد
- إدخال عنوان التوصيل
- قد تطبق رسوم توصيل

---

## 🔄 تدفق العمل

### من جانب الخياط:
1. يعمل على الطلب (measuring → cutting → sewing)
2. عند الانتهاء، يغير الحالة إلى **"جاهز"**
3. النظام يرسل الإشعارات تلقائياً

### من جانب العميل:
1. يتلقى إشعار عبر الواتساب والبريد
2. يفتح تفاصيل الطلب في التطبيق
3. يختار طريقة الاستلام
4. يؤكد الاختيار

---

## 🎯 الملفات المعنية

### 1. Types (`types.ts`)
```typescript
// أنواع جديدة
export type DeliveryMethod = 'pickup' | 'delivery';
export type NotificationChannel = 'whatsapp' | 'email' | 'sms';

// حقول جديدة في Order
interface Order {
  deliveryMethod?: DeliveryMethod;
  deliveryAddress?: string;
  readyNotificationSent?: boolean;
  readyNotificationDate?: string;
  notificationChannels?: NotificationChannel[];
}
```

### 2. خدمة الإشعارات (`services/notificationService.ts`)
- `sendWhatsApp()`: إرسال رسائل واتساب
- `sendEmail()`: إرسال بريد إلكتروني
- `sendSMS()`: إرسال رسائل نصية
- `sendOrderReadyNotification()`: إشعار الجاهزية
- `sendOrderStatusUpdate()`: تحديثات الحالة
- `sendPaymentReminder()`: تذكير بالدفع

### 3. صفحة تفاصيل الطلب (`pages/OrderDetails.tsx`)
- عرض حالة الإشعارات المرسلة
- modal اختيار طريقة الاستلام
- عرض طريقة الاستلام المختارة
- معالجة اختيار التوصيل/الاستلام

---

## 💡 كيفية الاستخدام

### للتطوير والاختبار:

1. **محاكاة طلب جاهز:**
```javascript
// في console المتصفح
const order = JSON.parse(localStorage.getItem('order_ord_xxxxx'));
order.status = 'ready';
localStorage.setItem('order_ord_xxxxx', JSON.stringify(order));
// أعد تحميل الصفحة
```

2. **مراقبة الإشعارات:**
- افتح Console في المتصفح
- ستظهر رسائل الإشعارات المرسلة
- تحقق من التفاصيل المسجلة

### للإنتاج:

1. **تفعيل واتساب API:**
```typescript
// في notificationService.ts
// قم بإضافة مفاتيح API الحقيقية
const response = await fetch('https://api.twilio.com/...', {
  method: 'POST',
  headers: { 
    'Authorization': `Bearer ${process.env.TWILIO_API_KEY}` 
  },
  // ...
});
```

2. **تفعيل خدمة البريد:**
```typescript
// استخدم SendGrid, AWS SES, أو Mailgun
const response = await fetch('https://api.sendgrid.com/v3/mail/send', {
  // ...
});
```

---

## 🎨 واجهة المستخدم

### 1. تنبيه جاهزية الطلب
- بطاقة خضراء بارزة
- أيقونة حزمة 📦
- نص واضح "طلبك جاهز!"
- عرض قنوات الإشعار المرسلة
- زر "اختر طريقة الاستلام"

### 2. Modal اختيار الاستلام
- خيارين واضحين بأيقونات
- تفاصيل كل خيار
- حقل إدخال العنوان للتوصيل
- تحقق من إدخال العنوان
- زر تأكيد وإلغاء

### 3. عرض الاختيار
- بطاقة ملونة تعرض الاختيار
- عنوان التوصيل إن وجد
- معلومات المحل للاستلام

---

## 🔐 الأمان والخصوصية

- يتم تخزين بيانات الاستلام محلياً
- الإشعارات تحتوي على معلومات أساسية فقط
- عناوين التوصيل مشفرة في الإنتاج
- التحقق من صحة رقم الهاتف والبريد

---

## 📊 المراحل والحالات

```
pending → measuring → cutting → sewing → ready → delivered
                                            ↓
                                    [إرسال إشعارات]
                                            ↓
                                    [اختيار الاستلام]
```

---

## 🚀 التحسينات المستقبلية

1. **تتبع التوصيل في الوقت الفعلي**
   - GPS للسائق
   - تحديثات لحظية

2. **جدولة الاستلام**
   - اختيار وقت الاستلام
   - تنبيهات قبل الموعد

3. **تقييم الخدمة**
   - تقييم التوصيل
   - ملاحظات العميل

4. **إشعارات دفع (Push Notifications)**
   - Firebase Cloud Messaging
   - إشعارات فورية

---

## 📞 الدعم الفني

للاستفسارات أو المشاكل التقنية:
- افتح issue في GitHub
- تواصل مع فريق التطوير
- راجع الوثائق الفنية

---

تم التطوير بواسطة فريق خيوط 🧵
